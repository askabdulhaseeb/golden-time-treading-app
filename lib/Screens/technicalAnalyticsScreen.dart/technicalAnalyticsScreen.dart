import 'package:flutter/material.dart';

class TechnicalAnalyticsScreen extends StatelessWidget {
  static final routeName = '/TechnicalAnalyticsScreen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Technical Analytics'),
      ),
      body: Center(
        child: Text('Technical Analytics'),
      ),
    );
  }
}
