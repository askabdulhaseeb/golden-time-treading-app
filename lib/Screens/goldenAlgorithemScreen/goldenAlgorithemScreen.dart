import 'package:flutter/material.dart';

class GoldenAlgorithemScreen extends StatelessWidget {
  static final routeName = '/GoldenAlgorithemScreen';
  @override
  Widget build(BuildContext context) {
    // white background
    return Scaffold(
      // app bar
      appBar: AppBar(
        title: Text('Golden Algorithem'),
      ),
      body: Center(
        child: Text('Golden Algorithem'),
      ),
    );
  }
}
