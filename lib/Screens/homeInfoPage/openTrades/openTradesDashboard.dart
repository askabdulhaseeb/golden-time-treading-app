import 'package:flutter/material.dart';

class OpenTradesDashBoard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("You Don't have any Open Traide now"),
    );
  }
}
